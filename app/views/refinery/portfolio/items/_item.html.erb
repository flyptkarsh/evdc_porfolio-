

<% if ::Refinery::Portfolio.display_item_text %>
  <div class='col-md-6 portfolio-item'>
    <h2><%= item.title %></h2>
    <p><%= raw item.caption %></p>
    <%= image_tag(item.image.url, {:id => dom_id(item), :title => item.title, width: '100%'}) %>
  </div>
<% end %>
